<Window x:Class="MyWinFormsApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="FreePOS - Login" Height="560" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="40">

            <!-- Logo -->
            <Border Width="64" Height="64" CornerRadius="32"
                    Background="{DynamicResource PrimaryHueMidBrush}"
                    HorizontalAlignment="Center" Margin="0,0,0,16">
                <materialDesign:PackIcon Kind="Storefront" Width="32" Height="32"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Foreground="White"/>
            </Border>

            <TextBlock Text="FreePOS" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,4"/>
            <TextBlock Text="Sign in to your account"
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       HorizontalAlignment="Center" Opacity="0.5" Margin="0,0,0,28"/>

            <!-- Email -->
            <TextBox x:Name="TxtEmail"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="Email"
                     FontSize="14" Margin="0,0,0,16"
                     KeyDown="Input_KeyDown"/>

            <!-- Password -->
            <PasswordBox x:Name="TxtPassword"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         materialDesign:HintAssist.Hint="Password"
                         FontSize="14" Margin="0,0,0,8"
                         KeyDown="Input_KeyDown"/>

            <!-- Error message -->
            <TextBlock x:Name="TxtError" Foreground="#F44336" FontSize="12"
                       TextWrapping="Wrap" Margin="0,0,0,16"
                       Visibility="Collapsed"/>

            <!-- Login button -->
            <Button x:Name="BtnLogin" Content="SIGN IN"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Height="40" FontSize="14"
                    Click="BtnLogin_Click"
                    HorizontalAlignment="Stretch"/>

            <ProgressBar x:Name="ProgressLogin"
                         Style="{StaticResource MaterialDesignCircularProgressBar}"
                         IsIndeterminate="True" Visibility="Collapsed"
                         HorizontalAlignment="Center" Margin="0,12,0,0"
                         Width="28" Height="28"/>

            <!-- Register link -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
                <TextBlock Text="Don't have an account? "
                           Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.6"
                           VerticalAlignment="Center"/>
                <Button x:Name="BtnGoRegister" Content="Register"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Padding="4,0" FontSize="13"
                        Click="BtnGoRegister_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
