<Window x:Class="MyWinFormsApp.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="FreePOS - Register" Height="660" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel VerticalAlignment="Center" Margin="40,30">

                <!-- Logo -->
                <Border Width="64" Height="64" CornerRadius="32"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        HorizontalAlignment="Center" Margin="0,0,0,16">
                    <materialDesign:PackIcon Kind="Storefront" Width="32" Height="32"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Foreground="White"/>
                </Border>

                <TextBlock Text="Create Account" FontSize="24" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,0,0,4"/>
                <TextBlock Text="Register your shop to get started"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           HorizontalAlignment="Center" Opacity="0.5" Margin="0,0,0,24"/>

                <!-- Shop Name -->
                <TextBox x:Name="TxtShopName"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Shop / Business Name"
                         FontSize="14" Margin="0,0,0,12"/>

                <!-- Full Name -->
                <TextBox x:Name="TxtFullName"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Your Full Name"
                         FontSize="14" Margin="0,0,0,12"/>

                <!-- Email -->
                <TextBox x:Name="TxtEmail"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Email"
                         FontSize="14" Margin="0,0,0,12"/>

                <!-- Password -->
                <PasswordBox x:Name="TxtPassword"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             materialDesign:HintAssist.Hint="Password"
                             FontSize="14" Margin="0,0,0,12"/>

                <!-- Confirm Password -->
                <PasswordBox x:Name="TxtConfirmPassword"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             materialDesign:HintAssist.Hint="Confirm Password"
                             FontSize="14" Margin="0,0,0,8"/>

                <!-- Error / Success message -->
                <TextBlock x:Name="TxtMessage" FontSize="12"
                           TextWrapping="Wrap" Margin="0,0,0,16"
                           Visibility="Collapsed"/>

                <!-- Register button -->
                <Button x:Name="BtnRegister" Content="CREATE ACCOUNT"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Height="40" FontSize="14"
                        Click="BtnRegister_Click"
                        HorizontalAlignment="Stretch"/>

                <ProgressBar x:Name="ProgressRegister"
                             Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True" Visibility="Collapsed"
                             HorizontalAlignment="Center" Margin="0,12,0,0"
                             Width="28" Height="28"/>

                <!-- Back to login -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock Text="Already have an account? "
                               Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.6"
                               VerticalAlignment="Center"/>
                    <Button x:Name="BtnGoLogin" Content="Sign In"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Padding="4,0" FontSize="13"
                            Click="BtnGoLogin_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
