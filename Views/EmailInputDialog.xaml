<Window x:Class="MyWinFormsApp.Views.EmailInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Email" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <StackPanel Margin="24">
        <TextBlock x:Name="TxtHeader" Text="Send by Email" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <TextBox x:Name="TxtEmail" FontSize="14"
                 materialDesign:HintAssist.Hint="Recipient Email Address"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="BtnCancel_Click"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="0,0,8,0" Height="36"/>
            <Button Content="Send" Click="BtnSend_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Height="36" Padding="20,6"/>
        </StackPanel>
    </StackPanel>
</Window>
