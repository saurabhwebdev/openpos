<UserControl x:Class="MyWinFormsApp.Views.InventorySuppliersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <!-- Layer 1: Supplier List -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="40,24,40,40">
                <materialDesign:Card Padding="24" UniformCornerRadius="12">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <materialDesign:PackIcon Kind="TruckDeliveryOutline" Width="20" Height="20"
                                                          VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.7"/>
                                <TextBlock Text="Suppliers" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" VerticalAlignment="Center"/>
                                <TextBlock x:Name="TxtCount" FontSize="12" Opacity="0.5" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Click="BtnImportExcel_Click" Height="36" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FileExcelOutline" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                        <TextBlock Text="Import Excel" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                        Click="BtnAdd_Click" Height="36">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                        <TextBlock Text="Add Supplier" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </DockPanel>

                        <!-- Search -->
                        <TextBox x:Name="TxtSearch" Margin="0,0,0,12"
                                 materialDesign:HintAssist.Hint="Search by name, contact, phone, GST..."
                                 materialDesign:HintAssist.IsFloating="False"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 FontSize="13" TextChanged="TxtSearch_TextChanged"/>

                        <!-- Header -->
                        <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Company" FontWeight="SemiBold" Opacity="0.6" FontSize="12"/>
                                <TextBlock Grid.Column="1" Text="Contact" FontWeight="SemiBold" Opacity="0.6" FontSize="12"/>
                                <TextBlock Grid.Column="2" Text="Phone" FontWeight="SemiBold" Opacity="0.6" FontSize="12"/>
                                <TextBlock Grid.Column="3" Text="City" FontWeight="SemiBold" Opacity="0.6" FontSize="12"/>
                                <TextBlock Grid.Column="4" Text="GST" FontWeight="SemiBold" Opacity="0.6" FontSize="12"/>
                                <TextBlock Grid.Column="5" Text="Active" FontWeight="SemiBold" Opacity="0.6" FontSize="12" HorizontalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <ItemsControl x:Name="SupplierList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1"
                                            Padding="0,10" Cursor="Hand" MouseLeftButtonUp="Row_Click" Tag="{Binding Id}"
                                            Background="Transparent">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="1.5*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="60"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="14" VerticalAlignment="Center" FontWeight="Medium"/>
                                            <TextBlock Grid.Column="1" Text="{Binding ContactPerson}" FontSize="12" VerticalAlignment="Center" Opacity="0.7"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Phone}" FontSize="12" VerticalAlignment="Center" Opacity="0.7"/>
                                            <TextBlock Grid.Column="3" Text="{Binding City}" FontSize="12" VerticalAlignment="Center" Opacity="0.7"/>
                                            <TextBlock Grid.Column="4" Text="{Binding GstNumber}" FontSize="11" VerticalAlignment="Center" Opacity="0.5"/>
                                            <Ellipse Grid.Column="5" Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="#4CAF50"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                <Setter Property="Fill" Value="#9E9E9E"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <ProgressBar x:Name="ProgressLoad" Style="{StaticResource MaterialDesignCircularProgressBar}"
                                     IsIndeterminate="True" Visibility="Collapsed" HorizontalAlignment="Center"
                                     Margin="0,20,0,0" Width="32" Height="32"/>
                        <TextBlock x:Name="TxtEmpty" Text="No suppliers yet. Add your first supplier."
                                   FontSize="12" Opacity="0.5" TextAlignment="Center" Margin="0,20,0,0" Visibility="Collapsed"/>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>

        <!-- Layer 2: Add/Edit Modal -->
        <Grid x:Name="OverlayEdit" Visibility="Collapsed">
            <materialDesign:Card x:Name="EditCard" MinWidth="700" Padding="32" UniformCornerRadius="16">
                <StackPanel>
                    <DockPanel Margin="0,0,0,16">
                        <TextBlock x:Name="TxtEditTitle" Text="Add Supplier"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}" DockPanel.Dock="Left"/>
                        <Button Style="{StaticResource MaterialDesignIconForegroundButton}" HorizontalAlignment="Right"
                                Click="BtnCancel_Click" Padding="4" Height="32" Width="32">
                            <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                        </Button>
                    </DockPanel>

                    <!-- Company Info -->
                    <TextBlock Text="COMPANY INFO" FontSize="11" FontWeight="SemiBold" Opacity="0.4" Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtName" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Company / Supplier Name *" FontSize="14" Margin="0,0,0,12"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtContactPerson" Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Contact Person" FontSize="14"/>
                        <TextBox x:Name="TxtPhone" Grid.Column="2" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Phone Number" FontSize="14"/>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtEmail" Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Email Address" FontSize="14"/>
                        <TextBox x:Name="TxtGstNumber" Grid.Column="2" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="GST Number" FontSize="14"/>
                    </Grid>

                    <!-- Address -->
                    <TextBlock Text="ADDRESS" FontSize="11" FontWeight="SemiBold" Opacity="0.4" Margin="0,8,0,8"/>
                    <TextBox x:Name="TxtAddress" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Address" FontSize="14" Margin="0,0,0,12"
                             TextWrapping="Wrap" MinHeight="50" AcceptsReturn="True"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtCity" Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="City" FontSize="14"/>
                        <TextBox x:Name="TxtState" Grid.Column="2" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="State" FontSize="14"/>
                        <TextBox x:Name="TxtPinCode" Grid.Column="4" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="PIN Code" FontSize="14"/>
                    </Grid>

                    <!-- Notes + Active -->
                    <TextBox x:Name="TxtNotes" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Notes (optional)" FontSize="14" Margin="0,0,0,12"
                             TextWrapping="Wrap" MinHeight="40" AcceptsReturn="True"/>

                    <CheckBox x:Name="ChkIsActive" Content="Active" IsChecked="True"
                              Style="{StaticResource MaterialDesignCheckBox}" Margin="0,0,0,12"/>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button x:Name="BtnSave" Style="{StaticResource MaterialDesignRaisedButton}"
                                Click="BtnSave_Click" Margin="0,0,12,0" Height="40">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ContentSave" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="SAVE" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnCancel_Click" Height="40">
                            <TextBlock Text="CANCEL"/>
                        </Button>
                        <Button x:Name="BtnDelete" Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnDelete_Click"
                                Height="40" Margin="12,0,0,0" Foreground="#F44336" BorderBrush="#F44336" Visibility="Collapsed">
                            <TextBlock Text="DELETE"/>
                        </Button>
                        <ProgressBar x:Name="ProgressSave" Style="{StaticResource MaterialDesignCircularProgressBar}"
                                     IsIndeterminate="True" Visibility="Collapsed" VerticalAlignment="Center"
                                     Width="28" Height="28" Margin="12,0,0,0"/>
                    </StackPanel>
                    <TextBlock x:Name="TxtMessage" FontSize="12" TextWrapping="Wrap" Margin="0,12,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
