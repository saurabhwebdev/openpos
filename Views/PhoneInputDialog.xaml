<Window x:Class="MyWinFormsApp.Views.PhoneInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Share on WhatsApp" Height="220" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <StackPanel Margin="24">
        <TextBlock Text="Share Invoice on WhatsApp" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock Text="Enter phone number with country code (e.g., +919876543210)"
                   FontSize="11" Opacity="0.5" Margin="0,0,0,12"/>

        <TextBox x:Name="TxtPhone" FontSize="14"
                 materialDesign:HintAssist.Hint="Phone Number"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="BtnCancel_Click"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="0,0,8,0" Height="36"/>
            <Button Content="Open WhatsApp" Click="BtnSend_Click"
                    Height="36" Padding="20,6"
                    Background="#25D366" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </StackPanel>
</Window>
