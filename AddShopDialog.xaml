<Window x:Class="MyWinFormsApp.AddShopDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Add New Shop" Height="220" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <StackPanel Margin="24">
        <TextBlock Text="Add New Shop" FontSize="18" FontWeight="Medium" Margin="0,0,0,16"/>

        <TextBox x:Name="TxtShopName"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 materialDesign:HintAssist.Hint="Shop / Business Name"
                 FontSize="14" Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CANCEL"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="BtnCancel_Click" Margin="0,0,8,0"/>
            <Button Content="CREATE"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="BtnCreate_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
